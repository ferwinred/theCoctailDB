export enum ERole {
    ADMIN= "admin",
    CLIENT= "client",
    BARTENDER="bartender",
    SUPERADMIN= "superadmin"
}

export enum RegisterRoles {
    CLIENT= "client",
    BARTENDER= "bartender",
}